import test from 'node:test';import assert from 'node:assert/strict';import {makePlatform,makeProject} from './test_utils.mjs';
test('approval inbox supports explicit human decision',()=>{const p=makePlatform();try{const x=makeProject(p);const a=p.governance.requestApproval(x.id,{kind:'MATERIAL_PURCHASE',subject:'Buy plywood',amount:5000000});assert.equal(p.governance.pendingApprovals().length,1);const d=p.governance.decideApproval(a.id,'APPROVED',{decidedBy:'owner'});assert.equal(d.status,'APPROVED');assert.equal(p.governance.pendingApprovals().length,0);}finally{p.close();}});
